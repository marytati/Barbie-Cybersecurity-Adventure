import secrets
 # For generating cryptographically secure random numbers
import strings 
# For accessing character sets like letters, digits, punctuation
# Function to generate a secure password
def generate_password(length=12):
    # Define character sets to use in the password
alphabet = (string.digits = string.punctuations) 
